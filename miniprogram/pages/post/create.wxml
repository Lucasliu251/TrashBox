<view class="container">
    <view class="header-section">
        <input class="title-input" placeholder="请输入标题..." placeholder-class="placeholder-style" maxlength="40" bindinput="onTitleInput" />
        <view class="word-count">{{title.length || 0}}/40</view>
    </view>
    <view class="divider"></view>
    <editor id="editor" class="ql-editor" placeholder="分享你的想法..." placeholder-class="editor-placeholder" bindready="onEditorReady" bindstatuschange="onStatusChange"></editor>
    <view class="toolbar" style="bottom: {{isIOS ? keyboardHeight : 0}}px">
        <view class="toolbar-left">
            <view class="tool-btn" catchtouchend="insertImage">
                <text class="iconfont icon-image">📷</text>
            </view>
            <view class="tool-btn {{formats.bold ? 'active' : ''}}" catchtouchend="toggleBold">
                <text class="iconfont icon-bold" style="font-weight: bold;">B</text>
            </view>
            <view class="tool-btn {{formats.blockquote ? 'active' : ''}}" catchtouchend="toggleQuote">
                <text class="iconfont icon-quote">“</text>
            </view>
        </view>
        <view class="publish-btn" bindtap="submitPost">发布</view>
    </view>
</view>